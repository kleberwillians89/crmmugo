# Interface em português

Valores técnicos persistidos continuam em inglês. A tradução visual é centralizada em `statusLabels.js`, `fieldLabels.js`, `navigationLabels.js` e `errorMessages.js`. Novos componentes devem usar esses mapas e nunca persistir o texto traduzido no lugar do enum.
