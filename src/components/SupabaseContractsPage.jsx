import {useEffect,useMemo,useState} from 'react'
import {ChevronRight,FileUp,Plus,X} from 'lucide-react'
import {PageHeader} from './PageHeader'
import {FeedbackMessage} from './FeedbackMessage'
import {createContract,generateContractInstallments,listContracts,updateContract} from '../services/data/contractsRepository'
import {listClients} from '../services/data/clientsRepository'
import {listProposals} from '../services/data/proposalsRepository'
import {useAuth} from '../contexts/AuthContext'
import {userError} from '../lib/userError'
import {contractFinancialStatus} from '../lib/contractBilling'
import {ContractBillingPreviewModal} from './ContractBillingPreviewModal'
import {SetupPaymentModal} from './SetupPaymentModal'
import {ServiceResponsibilitiesModal} from './ServiceResponsibilitiesModal'
import {ContractServiceResponsibilities} from './ContractServiceResponsibilities'
import {listActiveProfiles,updateServiceResponsibilities} from '../services/data/serviceResponsibilitiesRepository'
import {ContractRecordSections} from './ContractRecordSections'

const empty={client_id:'',proposal_id:'',responsible_id:'',contract_number:'',status:'pending_signature',signed:false,signed_at:'',start_date:'',end_date:'',minimum_term_months:'',billing_day:'',setup_value:'',setup_received_amount:0,monthly_value:'',total_value:'',auto_renew:false,notes:''}
const money=(value)=>Number(value||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})
const shortDate=(value)=>value?new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'2-digit'}).format(new Date(`${String(value).slice(0,10)}T12:00:00`)):'Não gerado'
const fullDate=(value)=>value?new Intl.DateTimeFormat('pt-BR',{day:'2-digit',month:'2-digit',year:'2-digit'}).format(new Date(`${String(value).slice(0,10)}T12:00:00`)):'Não informado'
const today=()=>new Date().toISOString().slice(0,10)

function ContractForm({form,setForm,clients,proposals,profiles,onSubmit,onCancel,editing}){
  function fromProposal(id){const proposal=proposals.find((item)=>item.id===id);setForm(proposal?{...empty,client_id:proposal.clientId,proposal_id:proposal.id,responsible_id:proposal.responsibleId||'',setup_value:proposal.setupValue||'',monthly_value:proposal.monthlyValue||'',total_value:proposal.totalValue||'',notes:'Criado a partir da proposta; revisar antes de salvar.'}:{...form,proposal_id:''})}
  return <form className="contract-drawer-form" onSubmit={onSubmit}><div className="form-grid"><label>Cliente<select required value={form.client_id||''} onChange={(event)=>setForm({...form,client_id:event.target.value})}><option value="">Selecione</option>{clients.map((client)=><option key={client.id} value={client.id}>{client.company_name}</option>)}</select></label><label>Responsável principal<select required value={form.responsible_id||''} onChange={(event)=>setForm({...form,responsible_id:event.target.value})}><option value="">Selecione</option>{profiles.map((member)=><option key={member.id} value={member.id}>{member.name}</option>)}</select></label><label>Proposta<select value={form.proposal_id||''} onChange={(event)=>fromProposal(event.target.value)}><option value="">Sem proposta</option>{proposals.filter((proposal)=>proposal.status==='won').map((proposal)=><option key={proposal.id} value={proposal.id}>{proposal.title}</option>)}</select></label><label>Número do contrato<input value={form.contract_number||''} onChange={(event)=>setForm({...form,contract_number:event.target.value})}/></label><label>Data inicial<input type="date" value={form.start_date||''} onChange={(event)=>setForm({...form,start_date:event.target.value})}/></label><label>Data final<input type="date" value={form.end_date||''} onChange={(event)=>setForm({...form,end_date:event.target.value})}/></label><label>Prazo mínimo em meses<input type="number" min="1" value={form.minimum_term_months||''} onChange={(event)=>setForm({...form,minimum_term_months:event.target.value})}/></label><label>Dia da cobrança<input type="number" min="1" max="31" value={form.billing_day||''} onChange={(event)=>setForm({...form,billing_day:event.target.value})}/></label><label>Setup contratado<input type="number" min="0" step="0.01" value={form.setup_value||''} onChange={(event)=>setForm({...form,setup_value:event.target.value})}/></label><label>Mensalidade<input type="number" min="0" step="0.01" value={form.monthly_value||''} onChange={(event)=>setForm({...form,monthly_value:event.target.value})}/></label><label>Total contratado<input type="number" min="0" step="0.01" value={form.total_value||''} onChange={(event)=>setForm({...form,total_value:event.target.value})}/></label><label>Status<select value={form.status} onChange={(event)=>setForm({...form,status:event.target.value})}>{['draft','pending_signature','active','expired','terminated','cancelled'].map((status)=><option key={status} value={status}>{{draft:'Rascunho',pending_signature:'Aguardando assinatura',active:'Ativo',expired:'Vencido',terminated:'Encerrado',cancelled:'Cancelado'}[status]}</option>)}</select></label><label>Assinado<select value={String(form.signed)} onChange={(event)=>setForm({...form,signed:event.target.value==='true',signed_at:event.target.value==='true'?(form.signed_at||today()):''})}><option value="false">Não</option><option value="true">Sim</option></select></label><label className="full-width">Observações<textarea value={form.notes||''} onChange={(event)=>setForm({...form,notes:event.target.value})}/></label></div><footer><button type="button" className="button secondary" onClick={onCancel}>Cancelar</button><button className="button">{editing?'Salvar alterações':'Criar contrato'}</button></footer></form>
}

function ExecutiveCards({contract}){const finance=contractFinancialStatus(contract),setupPending=Math.max(Number(contract.setup_value||0)-Number(contract.setup_received_amount||0),0),received=finance.received+Number(contract.setup_received_amount||0),balance=finance.balance+setupPending,hasOverdue=(contract.invoice_installments||[]).some((item)=>Number(item.amount||0)>Number(item.received_amount||0)&&item.due_date<today()),financialLabel=!finance.generated?'Cobranças não geradas':hasOverdue?'Em atraso':balance>0?'Em dia':'Recebido';return <><section className="contract-executive"><header><div><span className={`contract-status-dot ${contract.status}`}>● {contract.statusLabel}</span><h2>{contract.clients?.company_name||'Cliente não informado'}</h2><p>Contrato nº {contract.contract_number||'não informado'}</p></div></header><div className="contract-executive-values"><article><span>Mensalidade</span><strong>{money(contract.monthly_value)}</strong></article><article><span>Setup</span><strong>{money(contract.setup_value)}</strong></article><article><span>MRR</span><strong>{contract.status==='active'?money(contract.monthly_value):money(0)}</strong></article><article><span>Vigência</span><strong>{fullDate(contract.start_date)} → {fullDate(contract.end_date)}</strong></article></div></section><section className="contract-financial-section"><h3>Financeiro</h3><div className="contract-financial-grid"><article><span>Cobranças geradas</span><strong>{finance.generated}</strong></article><article><span>Recebido</span><strong>{money(received)}</strong></article><article><span>Saldo</span><strong>{money(balance)}</strong></article><article><span>Próximo vencimento</span><strong>{shortDate(finance.nextDue)}</strong></article><article><span>Status</span><strong>{financialLabel}</strong></article><article><span>Setup pendente</span><strong>{money(setupPending)}</strong></article></div></section></>}

export function SupabaseContractsPage(){
  const {canWrite,isAdmin}=useAuth(),[items,setItems]=useState([]),[clients,setClients]=useState([]),[proposals,setProposals]=useState([]),[profiles,setProfiles]=useState([]),[selected,setSelected]=useState(null),[form,setForm]=useState(empty),[editing,setEditing]=useState(false),[creating,setCreating]=useState(false),[editingService,setEditingService]=useState(null),[billing,setBilling]=useState(null),[payment,setPayment]=useState(null),[message,setMessage]=useState(''),[error,setError]=useState('')
  const load=async()=>{try{const rows=await listContracts();setItems(rows);setSelected((current)=>current?rows.find((row)=>row.id===current.id)||null:null)}catch(cause){setError(userError(cause))}}
  useEffect(()=>{Promise.all([listContracts(),listClients(),listProposals(),listActiveProfiles()]).then(([contractsRows,clientRows,proposalRows,profileRows])=>{setItems(contractsRows);setClients(clientRows);setProposals(proposalRows);setProfiles(profileRows)}).catch((cause)=>setError(userError(cause)))},[])
  const metrics=useMemo(()=>{const now=new Date(),month=now.toISOString().slice(0,7),active=items.filter((item)=>item.status==='active'),signed=items.filter((item)=>item.signed),renewals=active.filter((item)=>{if(!item.end_date)return false;const days=(new Date(`${item.end_date}T12:00:00`)-now)/86400000;return days>=0&&days<=60}),contractValue=(item)=>Number(item.total_value||0)||Number(item.setup_value||0)+Number(item.monthly_value||0)*Number(item.minimum_term_months||0);return{active:active.length,signed:signed.length,mrr:active.reduce((sum,item)=>sum+Number(item.monthly_value||0),0),contracted:items.filter((item)=>!['cancelled','terminated'].includes(item.status)).reduce((sum,item)=>sum+contractValue(item),0),forecast:items.reduce((sum,item)=>sum+contractFinancialStatus(item).balance+Math.max(Number(item.setup_value||0)-Number(item.setup_received_amount||0),0),0),signedMonth:signed.filter((item)=>String(item.signed_at||'').startsWith(month)).length,renewals:renewals.length}},[items])
  async function submit(event){event.preventDefault();try{const values=Object.fromEntries(Object.entries(form).map(([key,value])=>[key,value===''?null:value]));if(editing&&selected)await updateContract(selected.id,values);else await createContract(values);setCreating(false);setEditing(false);setForm(empty);setMessage(editing?'Contrato atualizado.':'Contrato criado.');await load()}catch(cause){setError(userError(cause))}}
  async function generate(includeHistory){try{const result=await generateContractInstallments(billing.id,includeHistory);setBilling(null);setMessage(`${result.created} cobrança(s) criada(s). Competências existentes foram preservadas.`);await load()}catch(cause){setError(userError(cause))}}
  async function saveSetup(values){try{await updateContract(payment.id,values);setPayment(null);await load()}catch(cause){setError(userError(cause))}}
  async function saveService(values){try{await updateServiceResponsibilities('contract_services',editingService.id,values);setEditingService(null);await load()}catch(cause){setError(userError(cause))}}
  function closeDrawer(){setSelected(null);setCreating(false);setEditing(false);setForm(empty)}
  function openEdit(){setForm({...empty,...selected,responsible_id:selected.responsibleId||selected.responsible_id||''});setEditing(true)}
  function navigate(page){window.dispatchEvent(new CustomEvent('mugo:navigate',{detail:page}))}
  return <div className="contracts-page premium-contract-hub"><PageHeader eyebrow="Receita contratada" title="Contratos" description="Painel executivo de contratos, vigências, cobranças e renovações." actions={<div className="contract-page-actions"><details className="import-menu"><summary className="button secondary"><FileUp size={16}/>Importar</summary><div>{['Proposta','Contrato','Aditivo','Documento'].map((label)=><button key={label} type="button" onClick={()=>navigate('documents')}>{label}</button>)}</div></details>{canWrite&&<button className="button" onClick={()=>{setCreating(true);setForm(empty)}}><Plus size={16}/>Novo contrato</button>}</div>}/>{message&&<FeedbackMessage type="success">{message}</FeedbackMessage>}{error&&<FeedbackMessage type="error">{error}</FeedbackMessage>}
    <section className="contract-portfolio-grid">{[['Contratos ativos',metrics.active],['Assinados',metrics.signed],['MRR',money(metrics.mrr)],['Receita contratada',money(metrics.contracted)],['Receita prevista',money(metrics.forecast)],['Assinados este mês',metrics.signedMonth],['Renovações',metrics.renewals]].map(([label,value])=><article key={label}><span>{label}</span><strong>{value}</strong></article>)}</section>
    <section className="contract-list-panel"><header><div><h2>Todos os contratos</h2><p>{items.length} registro(s) comerciais</p></div></header><div className="contract-list-head"><span>Cliente</span><span>Status</span><span>Mensalidade</span><span>Próximo vencimento</span><span>Responsável</span><span/></div><div className="contract-premium-list">{items.map((contract)=>{const finance=contractFinancialStatus(contract);return <button type="button" className="contract-premium-row" key={contract.id} onClick={()=>setSelected(contract)}><span className="contract-list-client"><strong>{contract.clients?.company_name||'Cliente não informado'}</strong><small>Contrato nº {contract.contract_number||'não informado'}</small></span><span><i className={`status-dot ${contract.status}`}/>{contract.statusLabel}</span><strong>{money(contract.monthly_value)}</strong><span>{shortDate(finance.nextDue)}</span><span>{contract.responsibleName||'Sem responsável'}</span><span className="open-contract">Abrir <ChevronRight size={15}/></span></button>})}{!items.length&&<div className="empty-state">Nenhum contrato cadastrado.</div>}</div></section>
    {(selected||creating)&&<><button className="contract-drawer-backdrop" aria-label="Fechar contrato" onClick={closeDrawer}/><aside className="contract-premium-drawer" role="dialog" aria-modal="true" aria-label={creating?'Novo contrato':`Contrato de ${selected?.clients?.company_name}`}><header className="drawer-topbar"><div><small>{creating?'Cadastro comercial':'Painel executivo'}</small><strong>{creating?'Novo contrato':selected?.clients?.company_name}</strong></div><button className="icon-button" onClick={closeDrawer} aria-label="Fechar"><X size={20}/></button></header><div className="contract-drawer-content">{creating||editing?<ContractForm form={form} setForm={setForm} clients={clients} proposals={proposals} profiles={profiles} onSubmit={submit} onCancel={()=>editing?setEditing(false):closeDrawer()} editing={editing}/>:<><ExecutiveCards contract={selected}/><ContractServiceResponsibilities services={selected.services} canWrite={canWrite} onEdit={setEditingService}/><ContractRecordSections contract={selected}/><div className="contract-drawer-actions">{canWrite&&<button className="button secondary" onClick={openEdit}>Editar contrato</button>}{canWrite&&<button className="button" onClick={()=>setBilling(selected)}>Gerar cobranças</button>}{canWrite&&Number(selected.setup_value)>Number(selected.setup_received_amount)&&<button className="button secondary" onClick={()=>setPayment(selected)}>Registrar setup</button>}<button className="button secondary" onClick={()=>navigate('finance')}>Abrir Financeiro</button></div></>}</div></aside></>}
    {billing&&<ContractBillingPreviewModal contract={billing} onClose={()=>setBilling(null)} onConfirm={generate}/>} {payment&&<SetupPaymentModal contract={payment} correction={isAdmin} onClose={()=>setPayment(null)} onSave={saveSetup}/>} {editingService&&<ServiceResponsibilitiesModal service={editingService} profiles={profiles} onClose={()=>setEditingService(null)} onSave={saveService}/>} </div>
}
