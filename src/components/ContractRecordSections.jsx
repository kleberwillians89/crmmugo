const money=(value)=>Number(value||0).toLocaleString('pt-BR',{style:'currency',currency:'BRL'})
export function ContractRecordSections({contract}){return <div className="contract-record-sections"><details open><summary>Dados do contrato</summary><dl className="details-list"><div><dt>Cliente</dt><dd>{contract.clientName}</dd></div><div><dt>Número</dt><dd>{contract.contractNumber}</dd></div><div><dt>Status</dt><dd>{contract.statusLabel}</dd></div><div><dt>Responsável principal</dt><dd>{contract.responsibleName||'Não informado'}</dd></div><div><dt>Início</dt><dd>{contract.startDate||'Não informado'}</dd></div><div><dt>Fim</dt><dd>{contract.endDate||'Sem data final'}</dd></div></dl></details><details open><summary>Financeiro</summary><dl className="details-list"><div><dt>Mensalidade</dt><dd>{money(contract.monthlyValue)}</dd></div><div><dt>Setup</dt><dd>{money(contract.setupValue)}</dd></div><div><dt>Total</dt><dd>{money(contract.totalValue)}</dd></div><div><dt>Dia de cobrança</dt><dd>{contract.billingDay||'Não informado'}</dd></div></dl></details><details open><summary>Documentos</summary>{(contract.documents||[]).map((item)=><p key={item.id}>{item.file_name} · {item.document_type}</p>)}{!contract.documents?.length&&<p>Nenhum documento relacionado.</p>}</details><details open><summary>Timeline</summary>{(contract.commercial_events||[]).map((item)=><p key={item.id}><strong>{item.title}</strong> · {String(item.created_at).slice(0,10)}<br/>{item.description}</p>)}{!contract.commercial_events?.length&&<p>Nenhum evento registrado.</p>}</details><details open><summary>Observações</summary><p>{contract.notes||'Nenhuma observação.'}</p></details></div>}

