import { useState } from 'react'
import { useAuth } from '../contexts/AuthContext'
import { FeedbackMessage } from './FeedbackMessage'
export function LoginPage(){const {signIn,error:accessError}=useAuth();const [email,setEmail]=useState('');const [password,setPassword]=useState('');const [error,setError]=useState('');const [loading,setLoading]=useState(false);async function submit(e){e.preventDefault();setLoading(true);setError('');const {error}=await signIn(email,password);if(error)setError('E-mail ou senha inválidos.');setLoading(false)}return <main className="login-page"><form className="login-card" onSubmit={submit}><div className="brand-mark">M</div><h1>Mugô CRM</h1><p>Acesso restrito à equipe autorizada.</p>{(error||accessError)&&<FeedbackMessage type="error">{error||accessError}</FeedbackMessage>}<label>E-mail<input type="email" required autoComplete="email" value={email} onChange={(e)=>setEmail(e.target.value)}/></label><label>Senha<input type="password" required autoComplete="current-password" value={password} onChange={(e)=>setPassword(e.target.value)}/></label><button className="button" disabled={loading}>{loading?'Entrando…':'Entrar'}</button></form></main>}
