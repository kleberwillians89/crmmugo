import {useEffect,useState} from 'react'
import {getTemporalContext} from '../lib/temporalIntelligence'
export function NowCard(){const [now,setNow]=useState(()=>new Date());useEffect(()=>{const timer=setInterval(()=>setNow(new Date()),30000);return()=>clearInterval(timer)},[]);const time=getTemporalContext(now);return <section className="now-card" aria-label="Momento atual"><span>Agora</span><div><strong>{time.weekday[0].toUpperCase()+time.weekday.slice(1)}</strong><b>{time.formattedDate}</b><b>{time.formattedTime}</b></div><p>{time.isHoliday?`Feriado: ${time.holidayName}`:time.isWeekend?'Fim de semana':`Horário comercial ${time.businessStatus}`}</p>{!time.isBusinessHours&&<small>Próximo expediente: {time.nextBusinessLabel} às {time.nextBusinessTime}</small>}</section>}
