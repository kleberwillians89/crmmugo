export const SERVICE_STATUS_LABELS={planned:'Planejado',negotiated:'Negociado',approved:'Aprovado',active:'Em execução',paused:'Pausado',completed:'Concluído',cancelled:'Cancelado',not_included:'Não contratado'}
const number=(value)=>value==null?null:Number(value)
const person=(value,id)=>value?{id:value.id||id,name:value.name||'Nome não informado'}:id?{id,name:'Responsável cadastrado'}:null
export function normalizeService(row={}){const status=row.service_status||row.status||'planned';return{id:row.id,name:row.service_name||row.serviceName||row.name||'Serviço não informado',serviceName:row.service_name||row.serviceName||row.name||'Serviço não informado',category:row.service_category||row.category||null,billingType:row.billing_type||row.billingType||null,setupValue:number(row.setup_value??row.setupValue),oneTimeValue:number(row.one_time_value??row.oneTimeValue),monthlyValue:number(row.monthly_value??row.monthlyValue),unitPrice:number(row.unit_price??row.unitPrice),quantity:number(row.quantity),discount:number(row.discount),totalValue:number(row.total_value??row.totalValue),durationMonths:number(row.duration_months??row.durationMonths),status,statusLabel:SERVICE_STATUS_LABELS[status]||'Status não informado',commercialResponsible:person(row.commercial_responsible||row.commercialResponsible,row.commercial_responsible_id||row.commercialResponsibleId),deliveryResponsible:person(row.delivery_responsible||row.deliveryResponsible,row.delivery_responsible_id||row.deliveryResponsibleId),supportResponsible:person(row.support_responsible||row.supportResponsible,row.support_responsible_id||row.supportResponsibleId),commercialResponsibleId:row.commercial_responsible_id??row.commercialResponsibleId??null,deliveryResponsibleId:row.delivery_responsible_id??row.deliveryResponsibleId??null,supportResponsibleId:row.support_responsible_id??row.supportResponsibleId??null,scopeSummary:row.scope_summary??row.scopeSummary??null,deliverables:row.deliverables??null,commercialNotes:row.commercial_notes??row.commercialNotes??null,operationalNotes:row.operational_notes??row.operationalNotes??null}}
export const normalizeServices=(rows=[])=>rows.map(normalizeService)

